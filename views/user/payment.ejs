<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Payment | Estate Pro</title>

<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">

<style>
  body {
    margin: 0;
    font-family: Inter, Arial, sans-serif;
    background: #f4f5f7;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
  }

  .payment-box {
    width: 420px;
    background: #ffffff;
    padding: 28px;
    border-radius: 8px;
    border: 1px solid #e5e7eb;
  }

  h2 {
    margin: 0 0 8px;
    font-size: 20px;
    font-weight: 600;
  }

  p {
    font-size: 13px;
    color: #6b7280;
    margin-bottom: 20px;
  }

  label {
    font-size: 13px;
    font-weight: 500;
    display: block;
    margin-bottom: 6px;
  }

  input, select {
    width: 100%;
    padding: 10px;
    border: 1px solid #d1d5db;
    border-radius: 4px;
    margin-bottom: 14px;
    font-size: 14px;
  }

  button {
    width: 100%;
    padding: 12px;
    background: #111827;
    color: white;
    border: none;
    border-radius: 4px;
    font-size: 14px;
    font-weight: 600;
    cursor: pointer;
  }

  button:hover {
    background: #000000;
  }

  .note {
    margin-top: 12px;
    font-size: 12px;
    color: #6b7280;
    text-align: center;
  }
  /* ================= TOP NAV ================= */
.navbar{
  background:rgba(255,255,255,0.75);
  backdrop-filter:blur(18px);
  border-bottom:1px solid var(--border);
  padding:18px 40px;
  display:flex;
  align-items:center;
  justify-content:space-between;
}

.brand{
  font-weight:600;
  letter-spacing:.5px;
}

.nav-links a{
  margin-left:24px;
  text-decoration:none;
  font-size:14px;
  color:var(--muted);
}

.nav-links a:hover{
  color:var(--primary);
}

</style>
</head>

<body>

<div class="navbar">
  <div class="brand">REAL ESTATE</div>

  <div class="nav-links">
    <a href="/user/dashboard">Dashboard</a>
    <a href="/user/sell">Sell</a>
    <a href="/properties">Buy</a>
    <a href="/user/my-properties">My Properties</a>
    <a href="/logout" style="color:#dc2626">Logout</a>
  </div>
</div>

<div class="payment-box">
  <h2>Payment Details</h2>
  <p>Complete payment to proceed with booking</p>

  <form method="POST" action="/payment/<%= bookingId %>">
    <label>Amount</label>
    <input type="number" name="amount" placeholder="Enter amount" required>

    <label>Payment Mode</label>
    <select name="payment_mode">
      <option value="UPI">UPI</option>
      <option value="Card">Card</option>
      <option value="NetBanking">Net Banking</option>
      <option value="Cash">Cash</option>
    </select>

    <button type="submit">Submit Payment</button>
  </form>

  <div class="note">
    Payment will be verified by the agent before confirming sale.
  </div>
</div>

</body>
</html>
