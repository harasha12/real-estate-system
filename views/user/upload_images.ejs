<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Upload Images | Estate Pro</title>

<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

<style>
  :root {
    --primary: #8b5cf6; /* Modern Violet */
    --primary-hover: #7c3aed;
    --bg-page: #f5f3ff;
    --text-main: #1e1b4b;
    --text-muted: #64748b;
  }

  body {
    font-family: 'Plus Jakarta Sans', sans-serif;
    background-color: var(--bg-page);
    margin: 0;
    min-height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 20px;
  }

  .upload-card {
    width: 100%;
    max-width: 450px;
    background: white;
    padding: 40px;
    border-radius: 28px;
    box-shadow: 0 20px 25px -5px rgba(139, 92, 246, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
    text-align: center;
  }

  .icon-header {
    width: 70px;
    height: 70px;
    background: #f5f3ff;
    color: var(--primary);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 28px;
    margin: 0 auto 20px;
    border: 2px dashed var(--primary);
  }

  h2 {
    font-size: 24px;
    font-weight: 800;
    color: var(--text-main);
    margin: 0 0 10px;
  }

  p {
    font-size: 14px;
    color: var(--text-muted);
    margin-bottom: 30px;
    line-height: 1.5;
  }

  /* File Input Styling */
  .drop-zone {
    position: relative;
    border: 2px dashed #e2e8f0;
    border-radius: 16px;
    padding: 40px 20px;
    background: #fafafa;
    transition: all 0.3s ease;
    cursor: pointer;
  }

  .drop-zone:hover {
    border-color: var(--primary);
    background: #fdfcff;
  }

  .drop-zone i {
    font-size: 32px;
    color: #cbd5e1;
    margin-bottom: 10px;
    display: block;
  }

  .drop-zone span {
    font-size: 14px;
    font-weight: 600;
    color: #94a3b8;
  }

  input[type="file"] {
    position: absolute;
    width: 100%;
    height: 100%;
    top: 0;
    left: 0;
    opacity: 0;
    cursor: pointer;
  }

  .file-info {
    margin-top: 15px;
    font-size: 12px;
    color: var(--primary);
    font-weight: 600;
    display: none; /* Initially hidden */
  }

  button {
    width: 100%;
    margin-top: 25px;
    padding: 16px;
    background: var(--primary);
    color: white;
    border: none;
    border-radius: 14px;
    font-size: 16px;
    font-weight: 700;
    cursor: pointer;
    box-shadow: 0 10px 15px -3px rgba(139, 92, 246, 0.3);
    transition: all 0.3s ease;
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 10px;
  }

  button:hover {
    background: var(--primary-hover);
    transform: translateY(-2px);
    box-shadow: 0 12px 20px -3px rgba(139, 92, 246, 0.4);
  }

  .back-link {
    display: inline-block;
    margin-top: 25px;
    color: var(--text-muted);
    text-decoration: none;
    font-size: 14px;
    font-weight: 600;
  }

  .back-link:hover {
    color: var(--primary);
  }
</style>
</head>
<body>

<div class="upload-card">
  <div class="icon-header">
    <i class="fa-solid fa-images"></i>
  </div>
  
  <h2>Property Gallery</h2>
  <p>Upload high-quality images to attract more buyers. You can select multiple files at once.</p>

  <form method="POST" action="/user/upload-images/<%= propertyId %>" enctype="multipart/form-data">
    <div class="drop-zone" id="dropZone">
      <i class="fa-solid fa-cloud-arrow-up"></i>
      <span>Click or Drag images here</span>
      <input type="file" name="images" id="fileInput" multiple required onchange="updateFileName()">
    </div>
    
    <div class="file-info" id="fileInfo"></div>

    <button type="submit">
      Start Upload 
      <i class="fa-solid fa-arrow-up-from-bracket"></i>
    </button>
  </form>

  <a href="/user/properties" class="back-link">
    <i class="fa-solid fa-chevron-left" style="font-size: 12px;"></i> Back to My Properties
  </a>
</div>

<script>
  function updateFileName() {
    const input = document.getElementById('fileInput');
    const info = document.getElementById('fileInfo');
    const count = input.files.length;
    
    if(count > 0) {
      info.style.display = 'block';
      info.innerHTML = `<i class="fa-solid fa-check"></i> ${count} files selected`;
      document.getElementById('dropZone').style.borderColor = 'var(--primary)';
    }
  }
</script>

</body>
</html>