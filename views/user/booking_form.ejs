<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Confirm Booking | Estate Pro</title>

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

<style>
  :root {
    /* Rich & Darker Color Palette */
    --primary: #4f46e5;
    --primary-glow: rgba(79, 70, 229, 0.4);
    --bg-dark: #0f172a; /* Deep Navy Blue */
    --bg-card: #ffffff;
    --text-main: #1e293b;
    --text-muted: #64748b;
    --border-color: #e2e8f0;
  }

  * {
    box-sizing: border-box;
    font-family: "Plus Jakarta Sans", sans-serif;
  }

  body {
    margin: 0;
    min-height: 100vh;
    /* Rich Gradient Background */
    background: radial-gradient(circle at top right, #1e1b4b, #0f172a);
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 20px;
    position: relative;
    overflow: hidden;
  }

  /* Decorative Background Elements */
  body::before {
    content: "";
    position: absolute;
    width: 300px;
    height: 300px;
    background: var(--primary);
    filter: blur(120px);
    border-radius: 50%;
    top: -100px;
    left: -100px;
    opacity: 0.2;
  }

  .booking-card {
    width: 100%;
    max-width: 450px;
    background: var(--bg-card);
    border-radius: 28px;
    padding: 45px;
    position: relative;
    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
    z-index: 1;
  }

  /* Top Accent Bar */
  .booking-card::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 8px;
    background: linear-gradient(90deg, #4f46e5, #ec4899);
    border-radius: 28px 28px 0 0;
  }

  .header-section {
    text-align: center;
    margin-bottom: 35px;
  }

  .booking-card h2 {
    margin: 0 0 10px;
    font-size: 28px;
    font-weight: 800;
    color: #0f172a;
    letter-spacing: -1px;
  }

  .booking-card p {
    font-size: 15px;
    color: var(--text-muted);
    margin: 0;
  }

  .form-group {
    margin-bottom: 22px;
  }

  label {
    display: block;
    font-size: 13px;
    font-weight: 700;
    margin-bottom: 8px;
    color: #334155;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .input-container {
    position: relative;
  }

  .input-container i {
    position: absolute;
    left: 16px;
    top: 50%;
    transform: translateY(-50%);
    color: #94a3b8;
    transition: color 0.3s;
  }

  input {
    width: 100%;
    padding: 14px 16px 14px 45px;
    border-radius: 12px;
    border: 2px solid #f1f5f9;
    font-size: 15px;
    background: #f8fafc;
    transition: all 0.3s ease;
    color: var(--text-main);
  }

  input:focus {
    outline: none;
    background: #fff;
    border-color: var(--primary);
    box-shadow: 0 0 0 4px var(--primary-glow);
  }

  input:focus + i {
    color: var(--primary);
  }

  .submit-btn {
    width: 100%;
    margin-top: 15px;
    padding: 16px;
    font-size: 16px;
    font-weight: 700;
    border-radius: 14px;
    border: none;
    background: #0f172a; /* Dark button for contrast */
    color: white;
    cursor: pointer;
    transition: all 0.3s ease;
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 10px;
  }

  .submit-btn:hover {
    background: var(--primary);
    transform: translateY(-3px);
    box-shadow: 0 10px 20px rgba(79, 70, 229, 0.3);
  }

  .note {
    margin-top: 25px;
    font-size: 12px;
    color: var(--text-muted);
    text-align: center;
    line-height: 1.6;
    padding: 0 10px;
  }

  .back-link {
    margin-top: 30px;
    text-align: center;
  }

  .back-link a {
    font-size: 14px;
    color: #64748b;
    text-decoration: none;
    font-weight: 600;
    transition: color 0.2s;
  }

  .back-link a:hover {
    color: var(--primary);
  }

  /* Animation for appearance */
  @keyframes slideUp {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .booking-card {
    animation: slideUp 0.6s ease-out;
  }
  /* ================= TOP NAV ================= */
.navbar{
  background:rgba(255,255,255,0.75);
  backdrop-filter:blur(18px);
  border-bottom:1px solid var(--border);
  padding:18px 40px;
  display:flex;
  align-items:center;
  justify-content:space-between;
}

.brand{
  font-weight:600;
  letter-spacing:.5px;
}

.nav-links a{
  margin-left:24px;
  text-decoration:none;
  font-size:14px;
  color:var(--muted);
}

.nav-links a:hover{
  color:var(--primary);
}

</style>
</head>

<body>

<div class="navbar">
  

<div class="booking-card">
  <div class="header-section">
    <h2>Book Property</h2>
    <p>Fill in the details to secure your spot</p>
  </div>

  <form method="POST" action="/booking/<%= property.id %>">
    <div class="form-group">
      <label for="name">Full Name</label>
      <div class="input-container">
        <i class="fa-solid fa-user"></i>
        <input id="name" name="name" type="text" placeholder="Ex: Rahul Sharma" required>
      </div>
    </div>

    <div class="form-group">
      <label for="phone">Phone Number</label>
      <div class="input-container">
        <i class="fa-solid fa-phone"></i>
        <input id="phone" name="phone" type="tel" placeholder="+91 XXXXX XXXXX" required>
      </div>
    </div>

    <div class="form-group">
      <label for="email">Email Address</label>
      <div class="input-container">
        <i class="fa-solid fa-envelope"></i>
        <input id="email" type="email" name="email" placeholder="rahul@example.com" required>
      </div>
    </div>

    <button type="submit" class="submit-btn">
      Confirm My Booking 
      <i class="fa-solid fa-arrow-right"></i>
    </button>
  </form>

  <div class="note">
    Secure process. Your information is encrypted and sent directly to the verified agent.
  </div>

  <div class="back-link">
    <a href="/properties"><i class="fa-solid fa-chevron-left" style="font-size: 10px; margin-right: 5px;"></i> Back to Properties</a>
  </div>
</div>

</body>
</html>