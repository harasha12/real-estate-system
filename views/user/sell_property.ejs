<!DOCTYPE html>
<html>
<head>
  <title>Sell Property</title>
  <style>
    :root{
      --bg:#f1f5f9;
      --card:#ffffff;
      --ink:#0f172a;
      --muted:#64748b;
      --primary:#2563eb;
      --accent:#f97316;
      --border:#e2e8f0;
    }

    *{ box-sizing:border-box; }

    body{ 
      margin:0;
      font-family: Arial, Helvetica, sans-serif;
      background:
        radial-gradient(circle at 0% 0%, #bfdbfe 0, transparent 55%),
        radial-gradient(circle at 100% 100%, #fee2e2 0, transparent 55%),
        var(--bg);
      color:var(--ink);
    }

    .box{
      width: 520px;
      margin: 40px auto;
      background:var(--card);
      padding:26px 26px 28px;
      border-radius:18px;
      box-shadow: 0 18px 45px rgba(15,23,42,0.16);
      position:relative;
      overflow:hidden;
    }

    .box::before{
      content:"";
      position:absolute;
      inset:-40%;
      background:
        radial-gradient(circle at 0 0,rgba(37,99,235,0.14),transparent 55%),
        radial-gradient(circle at 100% 100%,rgba(249,115,22,0.15),transparent 55%);
      opacity:0.22;
      pointer-events:none;
    }

    .box-inner{
      position:relative;
      z-index:1;
    }

    h2{
      text-align:center;
      margin:0 0 6px;
      font-size:22px;
      font-weight:600;
    }

    .subtitle{
      text-align:center;
      font-size:13px;
      color:var(--muted);
      margin-bottom:18px;
    }

    .tag-row{
      display:flex;
      justify-content:center;
      gap:8px;
      margin-bottom:12px;
      font-size:11px;
    }

    .tag{
      padding:4px 10px;
      border-radius:999px;
      background:#eef2ff;
      color:#4f46e5;
      border:1px solid #c7d2fe;
    }

    .field-label{
      font-size:13px;
      color:var(--muted);
      margin-top:10px;
      margin-bottom:2px;
    }

    input,
    textarea,
    select,
    button{
      width:100%;
      padding:11px 12px;
      margin-top:4px;
      border-radius:10px;
      border:1px solid var(--border);
      font-size:14px;
      font-family:inherit;
      transition:border-color .18s, box-shadow .18s, transform .12s;
      background:#f9fafb;
    }

    input:focus,
    textarea:focus,
    select:focus{
      outline:none;
      border-color:var(--primary);
      box-shadow:0 0 0 2px rgba(37,99,235,0.15);
      background:#ffffff;
    }

    textarea{
      resize:vertical;
      min-height:90px;
      max-height:200px;
    }

    button{
      background:linear-gradient(135deg,var(--primary),#1d4ed8);
      color:white;
      border:none;
      font-size:15px;
      font-weight:bold;
      cursor:pointer;
      margin-top:16px;
      box-shadow:0 16px 32px rgba(37,99,235,0.45);
    }

    button:hover{
      background:linear-gradient(135deg,#1d4ed8,#1e40af);
      transform:translateY(-1px);
      box-shadow:0 20px 38px rgba(37,99,235,0.55);
    }

    button:active{
      transform:translateY(0);
      box-shadow:0 10px 24px rgba(37,99,235,0.45);
    }
  </style>
</head>
<body>

<div class="box">
  <div class="box-inner">
    <h2>Sell / Rent Your Property</h2>
    <div class="subtitle">Share basic details so agents can evaluate and contact you quickly</div>

    <div class="tag-row">
      <div class="tag">Residential</div>
      <div class="tag">Commercial</div>
      <div class="tag">Land / Plots</div>
    </div>

    <form method="POST" action="/user/sell">

      <div class="field-label">Property Type</div>
      <select name="type" required>
        <option value="">Select property type</option>
        <option>Land</option>
        <option>House</option>
        <option>Apartment</option>
        <option>Agriculture</option>
      </select>

      <div class="field-label">Purpose</div>
      <select name="purpose" required>
        <option value="">Sell or Rent</option>
        <option value="Sell">Sell</option>
        <option value="Rent">Rent</option>
      </select>

      <div class="field-label">Location</div>
      <input name="location" placeholder="City / Area / Landmark" required>

      <div class="field-label">Expected Amount (₹)</div>
<input 
  type="number"
  name="market_amount" 
  placeholder="Your expected selling or monthly rent amount" 
  required
  inputmode="numeric"
  min="0"
  oninput="this.value = this.value.replace(/[^0-9]/g, '')"
/>


      <div class="field-label">Property Description</div>
      <textarea 
        name="description" 
        placeholder="Briefly describe size, facing, amenities,area development, age of property, etc."
      ></textarea>

      <button>Submit Property</button>
    </form>
  </div>
</div>

</body>
</html>