<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Buyer Enquiries | Estate Pro</title>

<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

<style>
  :root {
    --primary: #0284c7; /* Ocean Blue */
    --primary-light: #e0f2fe;
    --bg-page: #f1f5f9;
    --text-main: #1e293b;
    --text-muted: #64748b;
  }

  body {
    font-family: 'Plus Jakarta Sans', sans-serif;
    background-color: var(--bg-page);
    margin: 0;
    padding: 40px 20px;
    color: var(--text-main);
  }

  .container {
    max-width: 800px;
    margin: 0 auto;
  }

  .header {
    text-align: center;
    margin-bottom: 40px;
  }

  .header h2 {
    font-size: 28px;
    font-weight: 800;
    margin: 0;
    color: #0f172a;
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 12px;
  }

  .header h2 i {
    color: var(--primary);
  }

  /* Enquiry Card Styling */
  .enquiry-card {
    background: white;
    border-radius: 20px;
    padding: 25px;
    margin-bottom: 20px;
    border: 1px solid #e2e8f0;
    display: flex;
    flex-direction: column;
    gap: 15px;
    transition: all 0.3s ease;
    position: relative;
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
  }

  .enquiry-card:hover {
    transform: translateX(5px);
    border-color: var(--primary);
    box-shadow: 0 10px 15px -3px rgba(2, 132, 199, 0.1);
  }

  .buyer-profile {
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 1px solid #f1f5f9;
    padding-bottom: 15px;
  }

  .buyer-info {
    display: flex;
    align-items: center;
    gap: 15px;
  }

  .avatar {
    width: 50px;
    height: 50px;
    background: var(--primary-light);
    color: var(--primary);
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 20px;
    font-weight: 700;
  }

  .name-box h3 {
    margin: 0;
    font-size: 17px;
    color: var(--text-main);
  }

  .phone-link {
    font-size: 14px;
    color: var(--text-muted);
    text-decoration: none;
    display: flex;
    align-items: center;
    gap: 5px;
    margin-top: 2px;
  }

  .phone-link:hover {
    color: var(--primary);
  }

  /* Message Bubble Style */
  .message-box {
    background: #f8fafc;
    padding: 15px;
    border-radius: 12px;
    font-size: 14px;
    line-height: 1.6;
    color: #475569;
    border-left: 4px solid var(--primary);
  }

  .message-box b {
    display: block;
    font-size: 12px;
    text-transform: uppercase;
    color: var(--text-muted);
    margin-bottom: 5px;
    letter-spacing: 0.5px;
  }

  /* Action Buttons */
  .card-actions {
    display: flex;
    gap: 10px;
    margin-top: 5px;
  }

  .action-btn {
    flex: 1;
    padding: 10px;
    border-radius: 10px;
    text-decoration: none;
    font-size: 13px;
    font-weight: 700;
    text-align: center;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    transition: all 0.2s;
  }

  .btn-call {
    background-color: var(--primary);
    color: white;
  }

  .btn-call:hover {
    background-color: #0369a1;
  }

  .btn-whatsapp {
    background-color: #dcfce7;
    color: #166534;
  }

  .btn-whatsapp:hover {
    background-color: #22c55e;
    color: white;
  }

  /* Empty State */
  .no-enquiry {
    text-align: center;
    padding: 50px;
    background: white;
    border-radius: 20px;
    color: var(--text-muted);
  }

  @media (max-width: 480px) {
    .card-actions { flex-direction: column; }
  }
</style>
</head>
<body>

<div class="container">
  <div class="header">
    <a href="/agent/dashboard" style="text-decoration: none; color: var(--primary); font-weight: 600; font-size: 14px; display: block; margin-bottom: 10px;">
      <i class="fa-solid fa-arrow-left"></i> Dashboard
    </a>
    <h2><i class="fa-solid fa-comments-dollar"></i> Buyer Enquiries</h2>
  </div>

  <% if (enquiries && enquiries.length > 0) { %>
    <% enquiries.forEach(e => { %>
      <div class="enquiry-card">
        <div class="buyer-profile">
          <div class="buyer-info">
            <div class="avatar">
              <%= e.buyer_name.charAt(0).toUpperCase() %>
            </div>
            <div class="name-box">
              <h3><%= e.buyer_name %></h3>
              <a href="tel:<%= e.buyer_phone %>" class="phone-link">
                <i class="fa-solid fa-phone"></i> <%= e.buyer_phone %>
              </a>
            </div>
          </div>
          <div style="font-size: 12px; color: var(--text-muted);">
            <i class="fa-solid fa-clock-rotate-left"></i> New
          </div>
        </div>

        <div class="message-box">
          <b>Enquiry Message:</b>
          <%= e.message || "No message provided. Buyer is interested in this property." %>
        </div>

        <div class="card-actions">
          <a href="tel:<%= e.buyer_phone %>" class="action-btn btn-call">
            <i class="fa-solid fa-phone-flip"></i> Call Buyer
          </a>
          <a href="https://wa.me/<%= e.buyer_phone %>" target="_blank" class="action-btn btn-whatsapp">
            <i class="fa-brands fa-whatsapp"></i> WhatsApp
          </a>
        </div>
      </div>
    <% }) %>
  <% } else { %>
    <div class="no-enquiry">
      <i class="fa-solid fa-comment-slash" style="font-size: 40px; margin-bottom: 15px; opacity: 0.5;"></i>
      <p>No enquiries received yet.</p>
    </div>
  <% } %>
</div>

</body>
</html>